name: pulse_health
description: Pulse Health & Fitness App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  fl_chart: ^0.68.0
  google_fonts: ^6.1.0
  pedometer: ^4.0.0
  shared_preferences: ^2.2.0
  sensors_plus: ^4.0.0
  wakelock_plus: ^1.1.4
  image_picker: ^1.0.7
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true